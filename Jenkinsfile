pipeline {
    agent any
    stages {
        stage('Example Build') {
           steps {
               step{
                   
                   echo "Build"
               }

           }
        }
    }
}
